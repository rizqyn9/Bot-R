const {waFeed,waProblem} = require("./../setting/data/bot-setting.json")

// !Contact
const infoProblem = `Bila terjadi kesalahan silahkan hubungi : ${waProblem} as R-Dev`
const infoFeedback = `Untuk feedback dan selengkapya tentang Bot ini, bisa hubungi : ${waFeed} as *R-Dev*`

// !For Global

// !For personal

// !For Group


module.exports ={
    infoProblem,
    infoFeedback
}